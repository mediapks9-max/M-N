import { ModulePlaceholder } from "@/components/shell/module-placeholder";

export const metadata = { title: "Deliverables" };

export default function DeliverablesPage() {
  return (
    <ModulePlaceholder
      title="Deliverables"
      description="Every work output across engagements — articles, audits, websites, reports, videos — sorted by due date."
      phase="Phase B"
    />
  );
}
