import { ModulePlaceholder } from "@/components/shell/module-placeholder";

export const metadata = { title: "Services" };

export default function ServicesPage() {
  return (
    <ModulePlaceholder
      title="Services"
      description="Manage your service catalog: add, edit, reorder and deactivate the services you offer."
      phase="Phase B"
    />
  );
}
